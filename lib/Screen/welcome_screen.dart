import 'package:flutter/material.dart';

import 'package:todolist_app/Screen/homes.dart';

class WelcomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Homes(),
    );
  }
}
